# dependencies
node_modules/

# expo
.expo/
# DO NOT COMMIT: simulated home dir — contains browser profile, cookies, passwords, history, npm cache
.expo-home/
dist/
web-build/
expo-env.d.ts

# native builds
android/
ios/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# metro
.metro-health-check*

# env
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo

# test coverage
coverage/

# AI / IDE tooling
.claude/

# certificates / signing keys (never commit)
*.pem
*.pfx
*.crt
*.cert
*.keystore

# backups
*.bak
*.backup
*.orig

# catalogue build cache — raw Open Library responses, rebuilt on demand.
# `src/api/mock/catalog.json` (the output) IS committed.
scripts/.ol-cache.json

# logs (generic)
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
.pnpm-debug.*
lerna-debug.log*

# macOS / Windows
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# editors
.idea/
.vscode/*
!.vscode/extensions.json

# QA screenshot çıxışları — lokal olaraq yenidən yaradılır və 32 MB-dır.
# Repoda saxlansa hər EAS build-i onları yenidən yükləyir.
e2e/audit-*/
e2e/palettes/
e2e/screenshots/
